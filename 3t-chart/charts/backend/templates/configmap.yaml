apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-config
  namespace: {{ .Values.global.namespace | default .Release.Namespace }}
  labels:
    app: myapplication
data:
  DATABASE_URI: {{ .Values.configData.DATABASE_URI | quote }}
