apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backend.fullname" . }}-config
  namespace: 3t-app
  labels:
    app: myapplication
data:
  DATABASE_URI: {{ .Values.configData.DATABASE_URI | quote }}
